__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
build/
dist/
.DS_Store
*.tgz
tmp
.worktrees

# Local development, tests, docs, and deployment helpers are intentionally
# excluded from the published Hermes plugin checkout. Hermes installs this
# repository directly into $HERMES_HOME/plugins/clawchat, so keep GitHub's
# tracked tree focused on runtime plugin files only.
.e2e/
docs/
tests/
deploy/
scripts/
AGENTS.md
CLAUDE.md
