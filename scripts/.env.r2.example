# Cloudflare R2 credentials for `package_internal.sh` upload.
#
# Copy this file to `scripts/.env.r2` and fill in the real values. The real
# file is gitignored and must NOT be committed.

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=auto

R2_ENDPOINT=
R2_BUCKET=
